# Feature Status

Moved to [`docs/VERIFICATION.md`](VERIFICATION.md), which carries the
same per-subsystem table in CI's own status vocabulary (Verified in CI
/ Demonstrated / Partial / Experimental) instead of this page's
original Tested/Experimental/Known-gap wording — kept as one table
rather than two that could quietly drift apart.
